# Build output (installer binaries + generated catalog) — produced in CI.
/dist/

# Local build artifacts.
/tatsunari-sounds-installer
/tatsunari-sounds-installer.exe
*.exe

# NOTE: rsrc_windows_amd64.syso is NOT ignored — it is the committed Windows
# application manifest resource (see winres/README.md). It must ship in the
# repository so a plain `GOOS=windows go build` produces a manifested binary.
