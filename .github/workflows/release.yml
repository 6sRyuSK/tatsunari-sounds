name: Release

# Single consolidated release for the whole plugin factory.
#
# One GitHub Release per run (tag `<year>.<n>` — the n-th release of that year,
# e.g. 2026.1, 2026.2). Manually triggered from the Actions UI; no per-plugin
# tags, so the Releases page stays a single clean timeline.
#
# What a run does:
#   1. plan    — read every plugins/<slug>/plugin.toml version, compare against
#                the previous release's manifest.json, and build the list of
#                plugins whose version changed (single source of truth: plugin.toml).
#   2. build   — compile ONLY the changed plugins, on macOS (VST3 + AU) and
#                Windows (VST3), and zip each ON ITS NATIVE OS so bundle structure
#                and file permissions are preserved. Unchanged plugins are not rebuilt.
#   3. package — per OS, on the native runner: gather the changed plugins' zips
#                (this run) and the unchanged plugins' zips (copied verbatim from
#                the previous release), and assemble the per-OS "everything" bundle.
#                A macOS bundle is therefore only ever unzipped/rezipped on macOS —
#                never round-tripped through Linux — so it stays loadable.
#   4. release — publish one Release with every asset plus manifest.json (slug ->
#                version), which the next run reads to decide what to rebuild vs carry.
#
# The tag version comes from the release count, not from any single plugin, so
# the bundle has its own product-style version independent of per-plugin semver.

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  # --------------------------------------------------------------------------
  # Decide the tag and which plugins changed since the previous release.
  # --------------------------------------------------------------------------
  plan:
    name: Plan release
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    outputs:
      tag: ${{ steps.plan.outputs.tag }}
      prev_tag: ${{ steps.plan.outputs.prev_tag }}
      changed: ${{ steps.plan.outputs.changed }}
      include: ${{ steps.plan.outputs.include }}
      any_changed: ${{ steps.plan.outputs.any_changed }}
      plugins: ${{ steps.plan.outputs.plugins }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve tag, plugins and changed set
        id: plan
        shell: bash
        run: |
          set -euo pipefail

          # --- enumerate plugins: slug, version, display name, CMake target ---
          plugins='[]'
          for dir in plugins/*/; do
            slug=$(basename "$dir")
            [ -f "${dir}plugin.toml" ] || continue
            version=$(grep -oE 'version[[:space:]]*=[[:space:]]*"[0-9]+\.[0-9]+\.[0-9]+"' "${dir}plugin.toml" \
                      | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1)
            name=$(grep -oE 'name[[:space:]]*=[[:space:]]*"[^"]+"' "${dir}plugin.toml" \
                   | head -1 | sed -E 's/.*"([^"]+)".*/\1/')
            target=$(grep -oE 'juce_add_plugin\(\s*[A-Za-z0-9_]+' "${dir}CMakeLists.txt" \
                     | head -1 | sed -E 's/juce_add_plugin\(\s*//')
            if [ -z "$version" ] || [ -z "$target" ]; then
              echo "::error::Could not resolve version/target for $slug"; exit 1
            fi
            plugins=$(jq -c --arg s "$slug" --arg v "$version" --arg n "$name" --arg t "$target" \
                      '. + [{slug:$s, version:$v, name:$n, target:$t}]' <<<"$plugins")
          done
          echo "Plugins: $plugins"

          # --- find previous unified release (tag `<year>.<n>`) and its versions ---
          year=$(date +%Y)
          all_tags=$(gh api "repos/${GITHUB_REPOSITORY}/releases" --paginate --jq '.[].tag_name' 2>/dev/null || true)
          unified=$(printf '%s\n' "$all_tags" | grep -E '^[0-9]{4}\.[0-9]+$' || true)
          prev_tag=$(printf '%s\n' "$unified" | grep -E '.' | sort -t. -k1,1n -k2,2n | tail -1 || true)
          maxN=$(printf '%s\n' "$unified" | awk -F. -v y="$year" '$1==y{print $2}' | sort -n | tail -1)
          if [ -z "${maxN:-}" ]; then N=1; else N=$((maxN + 1)); fi
          tag="${year}.${N}"
          echo "prev_tag=${prev_tag:-<none>}  new tag=$tag"

          # previous manifest (slug -> version) drives change detection
          rm -rf prevrel; mkdir -p prevrel
          prev_manifest=""
          if [ -n "${prev_tag:-}" ]; then
            if gh release download "$prev_tag" -p manifest.json -D prevrel 2>/dev/null; then
              prev_manifest="prevrel/manifest.json"
            fi
          fi

          # --- changed set + build matrix (changed plugins x OS) ---
          changed='[]'
          include='[]'
          while read -r row; do
            slug=$(jq -r .slug <<<"$row")
            ver=$(jq -r .version <<<"$row")
            target=$(jq -r .target <<<"$row")
            pv=""
            if [ -n "$prev_manifest" ]; then
              pv=$(jq -r --arg s "$slug" '.[$s] // ""' "$prev_manifest")
            fi
            if [ "$pv" != "$ver" ]; then
              changed=$(jq -c --arg s "$slug" '. + [$s]' <<<"$changed")
              for os in macOS Windows; do
                include=$(jq -c --arg s "$slug" --arg t "$target" --arg v "$ver" --arg o "$os" \
                          '. + [{slug:$s, target:$t, version:$v, os:$o}]' <<<"$include")
              done
            fi
          done < <(jq -c '.[]' <<<"$plugins")

          any=false
          [ "$(jq 'length' <<<"$changed")" -gt 0 ] && any=true
          echo "Changed: $changed  (any=$any)"

          {
            echo "tag=$tag"
            echo "prev_tag=${prev_tag:-}"
            echo "changed=$changed"
            echo "include=$include"
            echo "any_changed=$any"
            echo "plugins=$plugins"
          } >> "$GITHUB_OUTPUT"

  # --------------------------------------------------------------------------
  # Build only the changed plugins and zip each on its native OS (one matrix
  # entry per (plugin, OS)). The zip — not a loose binary — is the artifact, so
  # macOS bundle structure and permissions survive the artifact hop intact.
  # --------------------------------------------------------------------------
  build:
    name: Build ${{ matrix.entry.slug }} (${{ matrix.entry.os }})
    needs: plan
    if: needs.plan.outputs.any_changed == 'true'
    strategy:
      fail-fast: false
      matrix:
        entry: ${{ fromJSON(needs.plan.outputs.include) }}
    runs-on: ${{ matrix.entry.os == 'macOS' && 'macos-latest' || 'windows-latest' }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Ninja (macOS)
        if: matrix.entry.os == 'macOS'
        run: brew install ninja

      - name: Set up MSVC + Ninja (Windows)
        if: matrix.entry.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install Ninja (Windows)
        if: matrix.entry.os == 'Windows'
        run: choco install ninja -y

      - name: Compiler cache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: release-${{ matrix.entry.os }}
          variant: ${{ matrix.entry.os == 'macOS' && 'ccache' || 'sccache' }}
          max-size: 1500M

      - name: Configure
        shell: bash
        run: |
          if [ "${{ matrix.entry.os }}" = "macOS" ]; then L=ccache; else L=sccache; fi
          cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER_LAUNCHER="$L" -DCMAKE_CXX_COMPILER_LAUNCHER="$L"

      - name: Build
        shell: bash
        run: |
          targets="${{ matrix.entry.target }}_VST3"
          if [ "${{ matrix.entry.os }}" = "macOS" ]; then
            targets="$targets ${{ matrix.entry.target }}_AU"
          fi
          cmake --build build --target $targets

      - name: Stage binaries
        shell: bash
        run: |
          mkdir -p out
          find "build/plugins/${{ matrix.entry.slug }}" \
            \( -type d -path '*/VST3/*.vst3' -o -type d -path '*/AU/*.component' \) \
            -exec cp -R {} out/ \;
          ls -la out

      - name: Zip plugin (macOS)
        if: matrix.entry.os == 'macOS'
        shell: bash
        run: |
          mkdir -p pkg
          ( cd out && zip -r -y -q "$GITHUB_WORKSPACE/pkg/${{ matrix.entry.slug }}-${{ matrix.entry.version }}-macOS.zip" . )

      - name: Zip plugin (Windows)
        if: matrix.entry.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force pkg | Out-Null
          Compress-Archive -Path out/* -DestinationPath "pkg/${{ matrix.entry.slug }}-${{ matrix.entry.version }}-Windows.zip" -Force

      - name: Upload plugin zip
        uses: actions/upload-artifact@v4
        with:
          name: pkg-${{ matrix.entry.os }}-${{ matrix.entry.slug }}
          path: pkg/*.zip
          if-no-files-found: error

  # --------------------------------------------------------------------------
  # Per OS, on the native runner: assemble the per-OS everything bundle from the
  # changed plugins' zips (this run) + the unchanged plugins' zips (verbatim from
  # the previous release). All unzip/rezip happens on the matching OS.
  # --------------------------------------------------------------------------
  package:
    name: Package ${{ matrix.os }}
    needs: [plan, build]
    if: always() && needs.plan.result == 'success' && (needs.build.result == 'success' || needs.build.result == 'skipped')
    strategy:
      fail-fast: false
      matrix:
        os: [macOS, Windows]
    runs-on: ${{ matrix.os == 'macOS' && 'macos-latest' || 'windows-latest' }}
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Download rebuilt plugin zips
        if: needs.plan.outputs.any_changed == 'true'
        uses: actions/download-artifact@v4
        with:
          pattern: pkg-${{ matrix.os }}-*
          path: built
          merge-multiple: true

      - name: Assemble bundle (macOS)
        if: matrix.os == 'macOS'
        shell: bash
        env:
          TAG: ${{ needs.plan.outputs.tag }}
          PREV_TAG: ${{ needs.plan.outputs.prev_tag }}
          PLUGINS: ${{ needs.plan.outputs.plugins }}
          CHANGED: ${{ needs.plan.outputs.changed }}
        run: |
          set -euo pipefail
          root="$PWD"; os="macOS"
          mkdir -p dist prev stage
          [ -n "$PREV_TAG" ] && gh release download "$PREV_TAG" -D prev || true
          is_changed() { jq -e --arg s "$1" 'index($s) != null' <<<"$CHANGED" >/dev/null; }
          while read -r row; do
            slug=$(jq -r .slug <<<"$row"); ver=$(jq -r .version <<<"$row")
            zipname="${slug}-${ver}-${os}.zip"
            if is_changed "$slug"; then
              [ -f "built/$zipname" ] || { echo "::error::missing rebuilt $zipname"; exit 1; }
              cp "built/$zipname" "dist/$zipname"
            else
              [ -f "prev/$zipname" ] || { echo "::error::carry-over asset missing: $zipname (prev=$PREV_TAG)"; exit 1; }
              cp "prev/$zipname" "dist/$zipname"
            fi
            mkdir -p "stage/$slug"
            ( cd "stage/$slug" && unzip -q "$root/dist/$zipname" )
          done < <(jq -c '.[]' <<<"$PLUGINS")
          ( cd stage && zip -r -y -q "$root/dist/tatsunari-plugins-${TAG}-${os}.zip" . )
          echo "----- dist ($os) -----"; ls -la dist

      - name: Assemble bundle (Windows)
        if: matrix.os == 'Windows'
        shell: pwsh
        env:
          TAG: ${{ needs.plan.outputs.tag }}
          PREV_TAG: ${{ needs.plan.outputs.prev_tag }}
          PLUGINS: ${{ needs.plan.outputs.plugins }}
          CHANGED: ${{ needs.plan.outputs.changed }}
        run: |
          $ErrorActionPreference = "Stop"
          $os = "Windows"
          New-Item -ItemType Directory -Force dist, prev, stage | Out-Null
          if ($env:PREV_TAG) { gh release download $env:PREV_TAG -D prev }
          $plugins = $env:PLUGINS | ConvertFrom-Json
          $changed = @($env:CHANGED | ConvertFrom-Json)
          foreach ($p in $plugins) {
            $zipname = "$($p.slug)-$($p.version)-$os.zip"
            if ($changed -contains $p.slug) {
              if (-not (Test-Path "built/$zipname")) { throw "missing rebuilt $zipname" }
              Copy-Item "built/$zipname" "dist/$zipname"
            } else {
              if (-not (Test-Path "prev/$zipname")) { throw "carry-over asset missing: $zipname (prev=$($env:PREV_TAG))" }
              Copy-Item "prev/$zipname" "dist/$zipname"
            }
            New-Item -ItemType Directory -Force "stage/$($p.slug)" | Out-Null
            Expand-Archive "dist/$zipname" -DestinationPath "stage/$($p.slug)" -Force
          }
          Compress-Archive -Path "stage/*" -DestinationPath "dist/tatsunari-plugins-$($env:TAG)-$os.zip" -Force
          Get-ChildItem dist

      - name: Upload per-OS assets
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.os }}
          path: dist/*.zip
          if-no-files-found: error

  # --------------------------------------------------------------------------
  # Publish the single Release: all per-OS assets + manifest.json + notes.
  # This job never touches binaries, only already-built zips and text.
  # --------------------------------------------------------------------------
  release:
    name: Publish Release
    needs: [plan, package]
    if: always() && needs.plan.result == 'success' && needs.package.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download per-OS assets
        uses: actions/download-artifact@v4
        with:
          pattern: dist-*
          path: dist
          merge-multiple: true

      - name: Build manifest and notes
        shell: bash
        env:
          TAG: ${{ needs.plan.outputs.tag }}
          PREV_TAG: ${{ needs.plan.outputs.prev_tag }}
          PLUGINS: ${{ needs.plan.outputs.plugins }}
          CHANGED: ${{ needs.plan.outputs.changed }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          plugins="$PLUGINS"; changed="$CHANGED"; tag="$TAG"; prev_tag="$PREV_TAG"
          is_changed() { jq -e --arg s "$1" 'index($s) != null' <<<"$changed" >/dev/null; }

          # manifest.json — slug -> version for every plugin (drives next run).
          jq -c 'map({(.slug): .version}) | add' <<<"$plugins" > dist/manifest.json

          # previous manifest for version-transition lines in the notes
          mkdir -p prev
          prev_manifest=""
          if [ -n "$prev_tag" ]; then
            if gh release download "$prev_tag" -p manifest.json -D prev 2>/dev/null; then
              prev_manifest="prev/manifest.json"
            fi
          fi

          {
            echo "## リリース ${tag}"
            if [ -n "$prev_tag" ]; then echo "_${prev_tag} からの変更_"; else echo "_初回リリース_"; fi
            echo
            echo "### 更新プラグイン"
            nchg=0
            while read -r row; do
              slug=$(jq -r .slug <<<"$row"); ver=$(jq -r .version <<<"$row"); name=$(jq -r .name <<<"$row")
              if is_changed "$slug"; then
                nchg=$((nchg + 1))
                pv=""
                [ -n "$prev_manifest" ] && pv=$(jq -r --arg s "$slug" '.[$s] // ""' "$prev_manifest")
                if [ -n "$pv" ]; then
                  echo "- ${name}: ${pv} → ${ver}"
                else
                  echo "- ${name}: ${ver}（新規収録）"
                fi
              fi
            done < <(jq -c '.[]' <<<"$plugins")
            [ "$nchg" -eq 0 ] && echo "- 変更されたプラグインはありません（再パッケージのみ）。"
            echo
            echo "### 収録プラグイン（全 $(jq 'length' <<<"$plugins") 本）"
            while read -r row; do
              name=$(jq -r .name <<<"$row"); ver=$(jq -r .version <<<"$row")
              echo "- ${name} ${ver}"
            done < <(jq -c '.[]' <<<"$plugins")
            echo
            echo "### ダウンロード"
            echo "- 全部入り: \`tatsunari-plugins-${tag}-macOS.zip\` / \`tatsunari-plugins-${tag}-Windows.zip\`"
            echo "- 個別プラグイン: \`<plugin>-<version>-macOS.zip\` / \`<plugin>-<version>-Windows.zip\`"
            echo
            echo "## インストール (macOS)"
            echo
            echo "リリースの macOS バイナリは**未署名・未notarize**です。ブラウザで zip を"
            echo "ダウンロードすると macOS が検疫属性 (\`com.apple.quarantine\`) を付けるため、"
            echo "Gatekeeper が「壊れているため開けません」と表示します（プラグインは壊れて"
            echo "いません）。インストール後にターミナルで検疫属性を外してください:"
            echo
            echo '```bash'
            echo "# <PluginName> はインストールした .component / .vst3 の実際の名前に置き換える"
            echo "# （ユーザー領域に入れた場合は /Library を ~/Library に、sudo は不要）"
            echo 'sudo xattr -dr com.apple.quarantine "/Library/Audio/Plug-Ins/Components/<PluginName>.component"'
            echo 'sudo xattr -dr com.apple.quarantine "/Library/Audio/Plug-Ins/VST3/<PluginName>.vst3"'
            echo '```'
            echo
            echo "その後 DAW を再起動して再スキャン（AU は \`killall -9 AudioComponentRegistrar\` の併用も有効）。"
          } > notes.md
          echo "----- notes.md -----"; cat notes.md

      - name: Publish
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.plan.outputs.tag }}
          name: ${{ needs.plan.outputs.tag }}
          files: |
            dist/*.zip
            dist/manifest.json
          draft: false
          body_path: notes.md
